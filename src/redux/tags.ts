export enum tags {
  blogs = 'blogs',
  bookings = 'bookings',
  categories = 'categories',
  services = 'services',
  faqs = 'faqs',
  feedbacks = 'feedbacks',
  reviews = 'reviews',
  topics = 'topics',
  users = 'users'
}

export const tagsArray: string[] = Object.values(tags)
